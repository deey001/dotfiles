# ble.sh configuration

# Disable paste confirmation - execute immediately on Enter
bleopt edit_abell=
bleopt exec_errexit_mark=
ble-bind -m emacs -f 'RET' 'accept-line'
ble-bind -m vi_imap -f 'RET' 'accept-line'
