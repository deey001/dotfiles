# .bash_wrappers: Custom function wrappers for commands
# This file defines functions that override or enhance built-in commands.

# Wrapper for cd to run ls after changing directory
function cd {
  builtin cd "$@" && ls  # Run cd and list files after changing directory
}
